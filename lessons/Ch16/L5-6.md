# TSP Review

Consider the `TSP` algorithms that we wrote:

### Solving TSP

```python
def tsp(cities, paths, dist):
    perms = permutations(cities)
    for perm in perms:
        total_dist = 0
        for i in range(1, len(perm)):
            total_dist += paths[perm[i - 1]][perm[i]]
        if total_dist < dist:
            return True
    return False
```

### Verifying TSP

```python
def verify_tsp(paths, dist, actual_path):
	total = 0
	for i in range(len(actual_path)):
		if i != 0:
			total += paths[actual_path[i-1]][actual_path[i]]
	return total < dist
```

---

### The Big O of 'tsp' is ____ and 'verify_tsp' is ____

- ( ) O(n), O(n!)
- (x) O(n!), O(n)
- ( ) O(2^n), O(1)
- ( ) O(1), O(n^2)

### The tsp function is in class...

- ( ) Underwater Basket Weaving 101
- ( ) P and NP
- ( ) P
- (x) NP