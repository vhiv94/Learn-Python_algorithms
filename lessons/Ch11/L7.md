# Fix Insert

Rotations are only useful if we can *use* them. When new nodes are inserted into the tree, they can break the red-black properties. We'll fix that by rotating the tree as new nodes are inserted, ensuring the tree remains balanced.

When we're done here, we will have a fully functional (albeit insert-only) red-black tree. As you can see if you look at the bottom of the test suite, we'll be inserting numbers into our tree ***in order***. A normal binary tree would break down into a single unruly branch:

```
                  > 7
               > 6
            > 5
         > 4
      > 3
   > 2
> 1
```

But our red-black tree will remain balanced:

```
      > 7
   > 6
      > 5
> 4
      > 3
   > 2
      > 1
```

## Assignment

In our implementation, we'll perform a "normal" insert, and then call a `fix_insert` method that will recolor and rotate the tree as necessary. This will ensure that the red-black properties are maintained.

1. [x] Complete the `insert` method to call our balancing method after inserting a new node.
2. [x] Complete the `fix_insert` method that maintains red-black tree properties, starting with the newly inserted node as the current node:
   1. [x] While the current node is not the root and has a red parent:
      1. [x] Identify the "parent", "grandparent", and "uncle" nodes of the newly inserted node
      2. [x] If the parent is a right child of the grandparent:
         1. [x] If the uncle is red:
            - [x] Recolor the uncle and parent to black
            - [x] Recolor the grandparent to red
            - [x] Move up the tree by making the current node the grandparent
         2. [x] If the uncle is black:
            - [x] If the current node is the left child of the parent:
               - [x] Move up the tree by making the current node the parent
               - [x] Rotate right around the current node
               - [x] Set the parent to be the current node's parent
            - [x] Recolor the parent to black
            - [x] Recolor the grandparent to red
            - [x] Rotate left around the grandparent
         3. [x] If the parent is a left child of the grandparent:
            1. [x] If the uncle is red:
               1. [x] Recolor the uncle and parent to black
               2. [x] Recolor the grandparent to red
               3. [x] Move up the tree by making the current node the grandparent
            2. [x] If the uncle is black:
               1. [x] If the current node is the right child of the parent:
                  - [x] Move up the tree by making the current node the parent
                  - [x] Rotate left around the current node
                  - [x] Set the parent to be the current node's parent
               - [x] Recolor the parent to black
               - [x] Recolor the grandparent to red
               - [x] Rotate right around the grandparent
   2. [x] Recolor the root to black





